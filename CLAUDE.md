# oss-review-lab

OSSのプルリクエスト(PR)のレビューコメントを教材にして、次の2つを学べるアプリ(および、その元になるデータ分析)を作る。

1. **PRでどんなコメントを書くべきか**(コメントの型と観点)
2. **良いコードの理想**(指摘の裏にある判断基準)

最終的な動機は、実務で使っているOSS(候補: NestJS、React)にコントリビュートすること。
副次的な目的として、TypeSafe AIのJevモデルの強みを、自分の手で検証してキャッチアップする。

## 現在のフェーズ

**フェーズ1: 対象OSSの選定調査(ほぼ完了・結論は暫定)。** `nestjs/nest`、`react/react`(旧 facebook/react)、参考で `prisma/orm` を比較した。Nest が教材の再現性と外部PRの通りやすさで優位だったため、**当面の対象OSSは `nestjs/nest`**(貢献の題材は未確定)。結果と限界は `.claude/doc/report.md`。

**いちばんやりたいのはJevのキャッチアップ。** OSSへの貢献より優先度が高い。フェーズ2は Jev Lab(Nestの判定済みデータでJevと小さなLLMを比べる)を先に進める想定。分類パイプライン、Webアプリは未着手・内容は未決。

## 実行制約(必ず守る)

- **読み取り専用。** PR・コメント・issueの投稿や変更を一切しない
- 取得は `gh` コマンド(公式API)のみ。GitHubのHTMLをスクレイピングしない
- トークンを出力・保存しない。`gh api rate_limit` を確認しながら進める
- 生データは `.claude/doc/data/` に置き、コミットしない
- 他人のコメントを全文転載しない。レポートやUIには「URL + 自分の言葉での1行要約 + 短い引用」だけ載せる

## 作業のルール

- 事実と推測を分けて書く。数字には出典(URLまたはコマンド)を添える
- 新しい依存関係やサービスを追加する前に、私に確認する
- サンプルの偏りや限界を、結論と一緒に必ず書く
- 分からないこと、未決のことは、勝手に決めず質問する
- 出力はMarkdownまたはCSV。コードは小さく、動く単位で進める

## 資料の索引

すべて `.claude/doc/` 配下(`.claude/` は `.gitignore` 済み)。該当する作業のときに読む。

| ファイル | 内容 | 読むタイミング |
|---|---|---|
| `report.md` | フェーズ1の比較レポート(付録AにPrisma) | 調査結果・数値・限界を確認するとき |
| `notes/data-collection.md` | 取得コマンド、データの癖(bot、外部判定、改名など) | データを取得・集計するとき |
| `notes/classification.md` | ラベル設計(観点・型・学びの大きさ)、フェーズ1の実績と次回の改善候補 | 分類・判定をするとき |
| `notes/repo-profiles.md` | Nest / React / Prisma ごとの傾向(PR・レビュー・貢献の入口) | 対象OSSを決めるとき、リポジトリの特徴を確認するとき |
| `notes/app-and-jev.md` | アプリ構想、Jevの仮説と検証(Jev Lab) | フェーズ2以降を設計するとき |
| `notes/legal.md` | 著作権・引用・個人情報の注意 | 掲載・公開を考えるとき |
| `notes/open-questions.md` | 未決事項と決定済み事項 | 方針を決める前に |
| `data/` | 生データ、`labels.csv`、`labels_prisma.csv`、`metrics.json`(コメント本文を含む) | 集計・再判定のとき |
| `labels/` | 判定の元テキスト | 判定を見直すとき |
| `*.py`, `fetch_rc.sh` | 取得・集計スクリプト(`.claude/doc` に `cd` して実行) | 再取得・再集計のとき |
